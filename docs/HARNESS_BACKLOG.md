# Harness Backlog

Durable backlog records are authoritative (`harness-cli query backlog --open`).
New items state title, discovery context, current pain, proposed improvement,
lane (`tiny`, `normal`, `high-risk`), predicted impact, and validation. This
markdown surface remains a review guide; do not duplicate operational rows here.
