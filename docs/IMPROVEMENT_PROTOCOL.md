# Improvement Protocol

Capture repeated ambiguity, manual work, weak proof, or drift in trace friction.
Use `harness-cli propose` to preview rule-based proposals; it writes nothing.
A human explicitly accepts or rejects one stable proposal. Accepted work gets
an intake, story, predicted impact, validation, and later measured outcome.
Never silently broaden a product task into a Harness redesign.
