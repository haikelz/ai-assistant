# Execution Plan

List discovery, contract preservation, incremental implementation, deterministic
tests, platform proof, rollback, and Harness updates. Stop if data safety,
provider behavior, or public contracts are ambiguous or proof is weakened.
