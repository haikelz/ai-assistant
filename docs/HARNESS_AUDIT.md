# Harness Audit

Run `harness-cli audit` to detect orphaned/stale stories, missing verification,
unmeasured completed improvements, and broken registered tools. Lower entropy is
better. Audit findings are evidence, not permission to mutate product code;
triage them through `IMPROVEMENT_PROTOCOL.md`.
